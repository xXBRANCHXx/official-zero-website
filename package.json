{
  "name": "official-zero-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "sync:routes": "node scripts/sync-root-routes.mjs",
    "generate:sitemaps": "node scripts/generate-sitemaps.mjs",
    "build": "node scripts/generate-sitemaps.mjs && node scripts/sync-root-routes.mjs && vite build && node scripts/clean-routes.mjs",
    "preview": "vite preview",
    "start": "vite"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "lenis": "^1.3.23"
  }
}
